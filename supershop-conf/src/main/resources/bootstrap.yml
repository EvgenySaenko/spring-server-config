spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          skip-ssl-validation: true
          uri: https://github.com/EvgenySaenko/spring-server-config.git
          force-pull: true